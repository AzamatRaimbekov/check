# Решение проблем с загрузкой проверок

## 🔍 Диагностика проблемы

Если проверки не загружаются на дашборде, выполните следующие шаги:

### 1. Проверьте консоль браузера

1. Откройте приложение в браузере: `http://localhost:5173`
2. Нажмите F12 для открытия DevTools
3. Перейдите на вкладку Console
4. Обновите страницу (F5)
5. Найдите сообщения:
   - `MockAPI: Initialized with` - должно показать количество данных
   - `Dashboard Debug:` - должно показать состояние загрузки
   - `MockAPI: getInspections called` - должно показать вызов API

### 2. Проверьте авторизацию

1. Убедитесь, что вы вошли в систему:
   - Email: `curator@example.com`
   - Пароль: `password123`
2. Если не авторизованы, вас должно перенаправить на страницу входа

### 3. Проверьте Network вкладку

1. Откройте вкладку Network в DevTools
2. Обновите страницу
3. Проверьте, есть ли ошибки HTTP запросов

### 4. Возможные причины проблем

#### Проблема 1: Приложение не запущено
**Решение:**
```bash
cd curator-app
npm run dev
```

#### Проблема 2: Ошибки в консоли JavaScript
**Решение:**
- Проверьте сообщения об ошибках в консоли
- Убедитесь, что все файлы загружены корректно

#### Проблема 3: React Query не работает
**Решение:**
- Проверьте, что QueryClientProvider обернул приложение
- Убедитесь, что useInspections хук вызывается правильно

#### Проблема 4: Mock данные не загружаются
**Решение:**
- Проверьте файл `src/data/mockData.ts`
- Убедитесь, что массив `inspections` содержит данные

### 5. Быстрое решение

Если ничего не помогает, попробуйте:

1. **Очистить кеш браузера:**
   - Ctrl+Shift+R (жесткое обновление)
   - Или очистить кеш в настройках браузера

2. **Перезапустить dev сервер:**
   ```bash
   # Остановить сервер (Ctrl+C)
   npm run dev
   ```

3. **Пересобрать проект:**
   ```bash
   npm run build
   npm run dev
   ```

### 6. Ожидаемое поведение

При правильной работе вы должны увидеть:
- 2 проверки на дашборде
- Одна "В процессе" (синяя)
- Одна "Завершено" (зеленая)
- Возможность кликнуть на проверку для перехода к чек-листу

### 7. Контакты для поддержки

Если проблема не решается:
1. Скопируйте сообщения из консоли браузера
2. Опишите шаги, которые привели к проблеме
3. Укажите браузер и его версию

---

**Совет:** Всегда проверяйте консоль браузера первым делом - там обычно есть подсказки о причине проблемы!
